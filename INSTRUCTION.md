# How to run and stop containers with docker-compose
Start all services defined in the docker-compose.yml file:
docker-compose up -d 
This will start the containers and return you to the command prompt.

Stop and remove containers, networks, and default volumes created by Compose:
docker-compose down
